require 'test_helper'

module RedisFeed
  describe Reader do

    context '#read' do
      it "must read from redis"
      it "must have defaults"
      it "must return hashes"
    end #read

  end
end
